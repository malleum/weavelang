; Nothing is embedded inside a Weave program today. This file exists so that
; the reverse works: a Markdown ```weave block picks the grammar up by its
; language name, which is what the file-types and scope in tree-sitter.json
; register.
