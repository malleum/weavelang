Copied from `../../tree-sitter-weave/queries` at build time by `make grammar`.
That directory is the source of truth; these are here so that installing the
plugin is enough for nvim-treesitter to find them, whichever way it was
installed. `TestQueriesAreInSync` fails if the two drift apart.
